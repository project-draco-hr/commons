{
  return String.format("Mean: %f, Min: %d, Max: %d, Range: %d, Stddev: %f, Variance: %f, " + "Population: %d, Sum: %d",mean(),min(),max(),range(),standardDeviation(),variance(),populationSize(),sum());
}
