def run_zinc_split(self, src_analysis_file, splits):
    zinc_split_args = ['-split', ','.join([('{%s}:%s' % (':'.join(x[0]), x[1])) for x in splits])]
    return self.run_zinc_analysis(src_analysis_file, zinc_split_args, workunit_name='split')
