@pytest.mark.skipif('sys.version_info > (3,)')
def test_pex_uncaught_exceptions():
    body = "raise Exception('This is an exception')"
    (so, rc) = run_test(body)
    assert ('This is an exception' in so), ('Standard out was: %s' % so)
    assert (rc == 1)
