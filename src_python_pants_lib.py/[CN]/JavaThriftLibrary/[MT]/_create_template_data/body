def _create_template_data(self):
    allsources = []
    if self.sources:
        allsources += list((os.path.join(JavaThriftLibrary._SRC_DIR, src) for src in self.sources))
    return ExportableJvmLibrary._create_template_data(self).extend(allsources=allsources)
