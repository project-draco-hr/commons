@classmethod
def scala_library(cls, path, name, deps=()):
    cls.create_target(path, dedent(("\n      scala_library(name='%(name)s',\n        dependencies=[%(deps)s],\n        sources=[],\n      )\n    " % dict(name=name, deps=','.join(deps)))))
