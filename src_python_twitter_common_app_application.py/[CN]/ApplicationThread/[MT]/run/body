@identify_thread
def run(self):
    try:
        self.__return_code = self.__main_method()
    except SystemExit as e:
        self.__return_code = e.code
    except Exception as e:
        self.__return_code = 1
        (_, _, self.__traceback) = sys.exc_info()
    self.stop()
