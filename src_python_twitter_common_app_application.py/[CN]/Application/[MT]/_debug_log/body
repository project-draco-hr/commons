def _debug_log(self, msg):
    if (hasattr(self._option_values, 'twitter_common_app_debug') and self._option_values.twitter_common_app_debug):
        print(('twitter.common.app debug: %s' % msg), file=sys.stderr)
