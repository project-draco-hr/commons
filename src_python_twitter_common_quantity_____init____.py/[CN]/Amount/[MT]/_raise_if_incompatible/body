def _raise_if_incompatible(self, other):
    if (type(self._unit) != type(other._unit)):
        raise TypeError(('Cannot convert from disparate base units: %s vs %s' % (type(self._unit), type(other._unit))))
