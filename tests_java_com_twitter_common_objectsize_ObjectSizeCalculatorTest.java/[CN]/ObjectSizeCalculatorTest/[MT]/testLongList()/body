{
  LinkedList<Object> l=new LinkedList<Object>();
  for (int i=0; i < 100000; ++i) {
    l.addLast(new Object());
  }
  assertSizeIs(roundTo(O + 4 + R,8) + roundTo(O + 3 * R,8) * 100001 + roundTo(O,8) * 100000,l);
}
