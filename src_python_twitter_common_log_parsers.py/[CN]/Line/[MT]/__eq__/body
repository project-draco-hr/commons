def __eq__(self, other):
    if (not isinstance(other, self.__class__)):
        return False
    return ((self.datetime == other.datetime) and (self.level == other.level) and (self.pid == other.pid) and (self.source == other.source) and (self.message == other.message))
