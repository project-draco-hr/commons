def _generate(self):
    env = self.chroot.dump()
    pex = PexBuilder(env)
    pex_name = os.path.join(self.distdir, ('%s.pex' % self.target.name))
    pex.write(pex_name)
    print(('Wrote %s' % pex_name))
