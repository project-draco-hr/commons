@classmethod
def setup_parser(cls, option_group, args, mkflag):
    option_group.add_option(mkflag('sep'), dest=('console_%s_separator' % cls.__name__), default='\\n', help='String to use to separate path results.')
