def __repr__(self):
    'Representation of ourselves in git reflog format'
    act = self.actor
    time = self.time
    return (self._fmt % (self.oldhexsha, self.newhexsha, act.name, act.email, time[0], altz_to_utctz_str(time[1]), self.message))
