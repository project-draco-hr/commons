def get_syms():
    r = {}
    vc = ParseContext.default_globals()
    for s in vc:
        if (s in PREDEFS):
            continue
        if s[0].isupper():
            continue
        o = vc[s]
        r[s] = o
    return r
