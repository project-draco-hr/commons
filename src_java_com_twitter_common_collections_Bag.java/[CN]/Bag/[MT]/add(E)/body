{
  long[] count=countedSet.get(o);
  if (count == null) {
    count=new long[1];
    count[0]=1;
    countedSet.put(o,count);
  }
 else {
    count[0]++;
  }
  totalFrequency++;
  return super.add(o);
}
