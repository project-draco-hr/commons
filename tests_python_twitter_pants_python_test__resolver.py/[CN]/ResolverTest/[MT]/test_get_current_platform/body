def test_get_current_platform(self):
    expected_platforms = [Platform.current(), 'linux-x86_64']
    self.assertEqual(set(expected_platforms), set(get_platforms(self.config.getlist('python-setup', 'platforms'))))
