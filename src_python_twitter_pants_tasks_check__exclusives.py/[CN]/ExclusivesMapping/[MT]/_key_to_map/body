def _key_to_map(self, key):
    result = {}
    if (key == '<none>'):
        return result
    pairs = key.split(',')
    for p in pairs:
        (k, v) = p.split('=')
        result[k] = v
    return result
