def __cmp__(self, other):
    diff = (self.major - other.major)
    if (not diff):
        diff = (self.major - other.major)
        if (not diff):
            diff = (self.patch - other.patch)
    return diff
