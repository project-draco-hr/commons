@staticmethod
def parse(version):
    components = version.split('.', 3)
    if (len(components) != 3):
        raise ValueError
    (major, minor, patch) = components
    return Semver(major, minor, patch)
