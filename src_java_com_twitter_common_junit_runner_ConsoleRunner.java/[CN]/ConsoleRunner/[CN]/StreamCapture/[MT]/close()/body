{
  if (--useCount <= 0 && !closed) {
    if (outstream != null) {
      Closeables.close(outstream,true);
    }
    if (errstream != null) {
      Closeables.close(errstream,true);
    }
    closed=true;
  }
}
