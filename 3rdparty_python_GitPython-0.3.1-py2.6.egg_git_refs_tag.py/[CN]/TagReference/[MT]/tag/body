@property
def tag(self):
    '\n\t\t:return: Tag object this tag ref points to or None in case \n\t\t\twe are a light weight tag'
    obj = self.object
    if (obj.type == 'tag'):
        return obj
    return None
