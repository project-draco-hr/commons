{
  try {
    return processingEnv.getFiler().createResource(StandardLocation.CLASS_OUTPUT,packageName,name);
  }
 catch (  IOException e) {
    error("Failed to create resource file to store %s/%s: %s",packageName,name,Throwables.getStackTraceAsString(e));
    return null;
  }
}
