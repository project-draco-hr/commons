@staticmethod
def fake(work):
    with temporary_dir() as td:
        with open(os.path.join(td, 'BUILD'), 'w') as fp:
            fp.write('# dummy BUILD file generated by pants\n')
            fp.flush()
            pc = ParseContext(BuildFile(td, 'BUILD'))
            return pc.do_in_context(work)
