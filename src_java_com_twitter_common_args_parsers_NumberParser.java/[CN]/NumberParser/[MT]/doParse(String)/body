{
  try {
    return parseNumber(raw);
  }
 catch (  NumberFormatException e) {
    throw new IllegalArgumentException(String.format("Invalid %s value: " + e.getMessage(),parsedClass.getSimpleName()));
  }
}
