def test_nodups_roots(self):
    targets = ([self.target('common/c')] * 2)
    self.assertEqual(2, len(targets))
    self.assert_console_output('overlaps/BUILD:two', 'common/c/BUILD:c', args=['--test-closed'], targets=targets)
