def _is_top_level(self, fn):
    (rel_fn_base, _) = os.path.splitext(self._relpath(fn))
    return any((((rel_fn_base == top_level) or rel_fn_base.startswith((top_level + '/'))) for top_level in self._top_levels))
