def __str__(self):
    return ('ServiceInstance(%s, addl: %s, status: %s)' % (self.service_endpoint, ' : '.join((('%s=>%s' % (key, val)) for (key, val) in self.additional_endpoints.items())), self.status))
