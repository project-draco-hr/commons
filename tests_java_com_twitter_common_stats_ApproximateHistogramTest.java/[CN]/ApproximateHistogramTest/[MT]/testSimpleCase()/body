{
  ApproximateHistogram hist=new ApproximateHistogram();
  int n=10;
  for (int i=0; i < n; i++) {
    hist.add(i);
  }
  for (int i=1; i <= n; i++) {
    double q=i / 10.0;
    assertEquals(i,hist.getQuantile(q),1.0);
  }
}
