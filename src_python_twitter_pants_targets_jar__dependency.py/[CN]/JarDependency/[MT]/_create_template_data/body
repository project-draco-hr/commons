def _create_template_data(self):
    return TemplateData(org=self.org, module=self.name, version=self.rev, excludes=self.excludes, transitive=self.transitive, ext=self.ext, url=self.url, configurations=';'.join(self._configurations))
