import os
import re
import sys
import time
import tempfile
import platform
from gitdb.util import make_sha, LockedFD, file_contents_ro, LazyMixin, to_hex_sha, to_bin_sha
__all__ = ('stream_copy', 'join_path', 'to_native_path_windows', 'to_native_path_linux', 'join_path_native', 'Stats', 'IndexFileSHA1Writer', 'Iterable', 'IterableList', 'BlockingLockFile', 'LockFile', 'Actor', 'get_user_id', 'assure_directory_exists', 'RemoteProgress')
if sys.platform.startswith('win'):
    to_native_path = to_native_path_windows
else:

    def to_native_path_linux(path):
        return path
    to_native_path = to_native_path_linux
