def is_compatible(self, filename):
    try:
        (_, _, egg_py_version, egg_platform) = self.parse(filename)
    except:
        return False
    (my_platform, my_arch, my_py_version) = self.get_architecture()
    if (egg_py_version and (egg_py_version != my_py_version)):
        return False
    if (egg_platform and (egg_platform[0] != my_platform)):
        return False
    return True
