def iterkeys(self):
    '\n        >>> ii = OrderedDict(((1, 3), (3, 2), (2, 1))).iterkeys()\n        >>> ii.next()\n        1\n        >>> ii.next()\n        3\n        >>> ii.next()\n        2\n        >>> ii.next()\n        Traceback (most recent call last):\n        StopIteration\n        '
    return iter(self._sequence)
